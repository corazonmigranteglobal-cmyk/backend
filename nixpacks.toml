[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["corepack enable", "corepack prepare yarn@1.22.22 --activate", "yarn install --frozen-lockfile"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn db:deploy && yarn start:prod"
